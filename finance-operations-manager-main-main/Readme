Finance Operations Manager
Description du projet
Ce projet est une application web de gestion financière construite avec React et Vite. Il est conçu pour être facilement déployé et géré à l'aide de Docker, en utilisant Nginx pour servir l'application en production.

Technologies utilisées
Frontend : React, TypeScript, Vite

Styling : Tailwind CSS

Conteneurisation : Docker, Docker Compose

Serveur de production : Nginx

Gestion des paquets : npm

Structure du projet
src/ : Le code source de l'application React.

dist/ : Le dossier de production généré par la commande npm run build.

docker/ : Contient les configurations Docker, y compris la configuration Nginx pour servir l'application frontend.

Dockerfile : Le fichier d'instructions pour créer l'image Docker de l'application frontend.

docker-compose.yml : Le fichier pour orchestrer les conteneurs (frontend, backend, base de données, etc.).

package.json : Définit les scripts de l'application et ses dépendances.

Démarrage du projet
🚀 Lancer l'application avec Docker (recommandé pour la production)
Pour construire les images Docker et démarrer tous les services définis dans docker-compose.yml, suivez ces étapes :

Assurez-vous que Docker Desktop est installé et en cours d'exécution sur votre machine.

Dans votre terminal, naviguez jusqu'à la racine du projet.

Exécutez la commande suivante pour construire les images et lancer les conteneurs :

docker-compose up -d --build

Le flag --build est important pour s'assurer que les dernières modifications sont prises en compte.

Le flag -d permet de lancer les conteneurs en arrière-plan.

L'application frontend sera accessible sur http://localhost:3000.

💻 Lancer l'application en développement
Si vous souhaitez travailler sur le projet sans Docker, vous pouvez le lancer en développement :

Installez les dépendances du projet :

npm install

Démarrez le serveur de développement Vite :

npm run dev

L'application sera accessible sur http://localhost:5173.

Commandes utiles
docker-compose up -d --build : Construit et démarre les conteneurs en arrière-plan.

docker-compose down : Arrête et supprime les conteneurs.

npm run build : Crée la version optimisée de l'application dans le dossier dist.

npm run dev : Démarre le serveur de développement.